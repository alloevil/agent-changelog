name: Sync Changelog

on:
  workflow_dispatch: # Manual trigger only (semi-automatic mode)

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Sync from OpenClaw releases
        run: node scripts/sync.mjs
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ANTHROPIC_BASE_URL: ${{ secrets.ANTHROPIC_BASE_URL }}
          ANTHROPIC_AUTH_TOKEN: ${{ secrets.ANTHROPIC_AUTH_TOKEN }}

      # sync.mjs writes openclaw_data.js and then re-renders the static release
      # archive inside the project pages, so those are the files that move.
      - name: Check for changes
        id: changes
        run: |
          if git diff --quiet openclaw_data.js hermes_data.js openclaw.html hermes.html; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit and push
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add openclaw_data.js hermes_data.js openclaw.html hermes.html
          git commit -m "chore: sync changelog from OpenClaw"
          git push
